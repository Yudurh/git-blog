public class ex22 {
    public static void main(String[] args) {

        //조건문 if문 switch문
        //조건문 : true/false 조건에 따라서 선택적 실행 가능
        // 조건식 : 비교논리연산자로 이루어진 식(절)

        //if문의 형식
        //if( 조건식 ) {
        //   true 일때 수행되는 실행문
        //  }

        int i = 20;
        if (i == 20) {
            System.out.println("i가 20임");
        }

        if (i == 10); {
            System.out.println("i가 10임");
        }


        //영역(Scope) = 블록(Block)
        //클래스,함수,조건문,반복문,배열초기화에 사용하고
        //해당하는 예약어의 영역을 지정하는데 사용
        //1.한 depth(깊이)가 들어가면 들여쓰기/내어쓰기(4칸)을 해줌.
        //2.변수,함수,클래스 이름을 파악하기 쉽게 해줌
        //가독성을 확보하기 위해 사용

        //고객에게 주는 생일 쿠폰 포인트

        int p = 1000;
        int point = 1000;
        int pointOfCustomerBirthday = 1000;



    }
}
